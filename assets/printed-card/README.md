# 实体印刷母版

**当前不用再打印。** 手里的插画底卡和原二维码继续使用。

本地同目录的 `嘉地-品丽珠_立牌01.png` 与 `嘉地-品丽珠_立牌01.svg` 是恢复的完整印刷母版，未改内容。旧名称不代表当前摆法：底卡和试管现在都横放。源文件留在本地，不上传GitHub；尺寸、哈希与二维码记录见 [manifest.json](manifest.json)。

以后需要印刷时审核这两份完整母版。不要打印 `site/ar/image-targets/` 的灰度裁片，也不要把虚拟品鉴卡贴图当作这张纸的源文件。

只换动画无需重印。若以后修改实体特征/尺寸，需要同步定位数据并实测，不在当前动画制作中夹带此变更。
