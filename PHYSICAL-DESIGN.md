# 实体 marker 与物料方案

## 决定

不把 29 mm 宽的现有圆柱正标作为 30–50 cm 体验的必经识别目标，也不再把“先让用户试试曲面示例”当下一步。当前推荐：一体式竖向平面背卡，左侧完整图像目标，右侧试管固定槽；用物料确定目标与管体的相对关系，8th Wall 原生平面目标负责相机定位。

这是一份尺寸明确、可打样的工程方案；不是已经通过真实手机验收的成品。

## 比较和选择

| 方案 | 物理特点 | 决定 |
| --- | --- | --- |
| 现有窄正标 / 新窄腰封 | 正面横向投影最多约29 mm；还包含曲率、细字与反光 | 不作为主路径；官方支持 cylinder 不代表该距离成立 |
| 脚底水平二维码/图案 | 与整管观看角度冲突，容易要求俯视/靠近 | 淘汰 |
| 竖向平面背卡、左右两条窄图案、管居中 | 视觉对称，但中心遮挡，图像目标被分割 | 不选作首个工程布局 |
| 竖向平面背卡、完整图像区在侧面、试管固定在另一侧 | 目标宽而连续，镜头同时包含目标与管体；纸张与瓶底无独立自由偏移 | 推荐 |

光学尺度估算（不是识别阈值）：29 mm 在300/500 mm距离张角约5.5°/3.3°；120 mm约22.6°/13.7°。同距横向采样量约4.1倍；曲面可用纹理还会受压缩。不借用其他引擎的经验值冒充8th Wall保证。

8th Wall 官方要求丰富多样细节、灰度对比，避免重复纹理/大空白，且指出反光会降低质量：https://8thwall.org/docs/engine/guides/image-targets 。所以优先扩大并保护连续可见的图像区域，不添加随机黑块或依赖二维码密度。

## 拟定首个样机尺寸（单位mm，原点为成品背卡左上）

- 背卡：180宽 ×270高，A4可100%打印；哑光、平整，贴在现有夹板上。此尺寸是新布局决定，不复用旧图稿。
- 顶部夹具避让区：y=0..45。不把必需特征放在这里。
- 连续追踪图像：x=10..130，y=65..225，120×160，3:4竖幅。用高质量插画自身作特征，轮廓/明暗不依赖颜色区分。此矩形是目标裁切框，不是必须印出黑边。
- 试管预留：x=140..175，y=40..260，35×220。轴线x=157.5；以底托承托面y=255为基准，215 mm管体顶部y=40。卡槽尺寸要用真实管体公差验证，29 mm是标称值。
- 固定：底托承重，上下两个薄卡扣/透明固定带限制左右、前后与倾斜。打印模版定义底托/固定带落点；不能只有画出来的槽。制作者或现场工作人员按模版装一次，消费者不装配、不输入尺寸。
- QR预留：左下x=10..42，y=232..264，32×32（包含静区）；实际模块数/URL确定后用扫码验证决定是否放大。QR只进入网页，位于管体外侧。
- 实际印刷可包含标题、酒款、风土与插画，但不能挤占上述追踪矩形、槽与夹具避让区。

完整120 mm图像区与管体有10 mm横向间隔；管半径约14.5 mm，适度左右视角下仍需验证侧向投影遮挡。测试先按50 cm正面及左右约20°，30 cm是覆盖范围检查，不能变成让消费者靠近的补救要求。艺术完成前先检查整卡、管顶与预留动画空间的手机取景。

## 为什么无需点瓶底/瓶盖

卡片印刷尺寸定义尺度，原生平面追踪给出卡片在相机中的姿态，底托与固定件定义管体在卡片坐标系的位置；两者组合就是酒管位置。纸、槽、托、管作为一个组装物，不再是可任意相对滑动的两件东西。

这是产品物料承担合理的几何职责，不是消费者校准。若不接受任何固定结构而又要试管任意摆放，技术任务会变成额外的物体姿态估计；不把这种能力偷偷写成已经具备。

## 不能被省略的 presence 关卡

平面图像跟踪本身无法证明槽内有管；固定关系也不是存在检测。空卡不播放的要求保留。

拟采用单独的槽内占位判断：在固定槽中安排可观察的印刷参考区域，结合卡片姿态，把相机中的槽区域与空槽基准比较；同时检查槽外参考区，避免把全局昏暗/失焦当成有管。沿多个高度连续观察纵向占位，手短暂经过不立即触发。只判“有合理物体占位”，不做SKU/真伪认证。

这是待实现、待真机验证的应用层判据，不是8th Wall自带通用物体检测，也不能把单纯“没看见小标记”当作有管。已知底托/卡扣解决姿态，presence只决定开演；它不负责重新解算任意酒管坐标。若空槽/手遮挡/管离开仍不能可靠区分，必须报告失败，不用按钮或默认已放管掩盖。

## 动画接口

动画挂在确定的管体坐标下，使用约215×29 mm遮挡代理保证前后关系。首个创意输出是完整管体旁的构图和短片预演，不是直接在AR中散放食材。QR打开SKU清单，清单绑定目标版本、物理尺寸、底托变换、动画资源；初期静态HTTPS即可，无需先搭业务后台。

## 当前需要用户做什么

现在不要求用户打印、测距、点选标记或提供engine判断。下一产物由assistant完成：包含前/侧视关系的装配设计、手机视野中的构图，以及动画分镜/预演。人工需要做的是确认这件物料符合现场交付方式、审美方向；随后执行一次明确的实物验收，而不是替开发者从零摸索识别距离。

真实可用性必须联合验证：50 cm无人工定位自动开演；整管可见；顶部夹遮挡；左右观看贴合；空槽不播放；移走/放回无需校准；已完成美术版本的灰度/印刷/光照效果；目标手机/微信兼容。以上是验收条件，不是已通过成绩。
