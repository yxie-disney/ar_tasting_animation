# 当前实体基准：复用用户已打印的卡片

2026-09-18 用户确认：以 `C:/Users/rebel/Downloads/Image_20260918172937_137_1.jpg` 中的实物、横放布局和自然斜俯视拍摄位置为准，完整试管和右侧二维码同时可见。观看距离目标为30–50cm；不从单张照片推断精确相机角度、距离或酒管坐标。

## 不再改变的本轮输入

- 不要求用户重印，复用手中这张卡和原二维码。
- 原 PNG / SVG 已从清理前备份逐字节恢复到 `assets/printed-card/`，SHA-256与备份一致。旧文件名中的“立牌”只是历史名称，不要求竖直使用。
- 原稿为180×270mm、PNG为2126×3189px；横放使用相当于270×180mm名义卡面。原稿尺寸不等于重新测量过的实物尺寸，不据此擅自缩放或重排。
- 原二维码从恢复的PNG解码核验为 `https://yxie-disney.github.io/ar_tasting_animation/v3/`。后续体验沿用该入口，不创建新二维码或要求用户手动改网址。
- 酒管215×Ø29mm。照片中的替代SKU可以参与测试，不要求换成嘉地正标；本卡对应嘉地体验，不依据替代管正标改变SKU。
- 不新增鞍座、止挡、垫高或新的120×90mm目标区；此前270×190mm托盘方案和layout文件不是执行输入。

## 实施边界

沿用原卡可见插画承担平面定位，二维码承担入口。追踪资源及应用代码可更新，纸张不变。不以细窄酒标识别、精确瓶身姿态重建或消费者手动登记为前提。当前实现保留浅色中央区域的宽松深色占用判断，显示内容为静态虚拟品鉴卡；这不是通用物体检测或动态瓶身姿态估计。

**阅读关系：虚拟卡在后、试管在前、手机和人在更前。** 试管仍横躺在纸上；虚拟卡垂直纸面，正面朝初次观看者，顶边沿纸面法线向上，原稿横排文字不改。不能将实体的平放要求继承成虚拟阅读面的平放要求。初次摆放完成后不随手机滚转或持续转向镜头。

本轮按用户明确要求，将完整原卡放大到固定名义尺寸 216 × 477.4mm，用于观察虚拟空间边界。不改图、不重排、不生图；允许画面边缘出框，不用自动缩小来保证整卡入镜。实体印刷物没有变化。

原 `/v3/` 入口部署当前自动体验，旧手动定位页面不恢复。实现通过真实引擎的照片视频回放集成检查与数字空卡对照，细节见 `tests/verification.md`。照片也参与了印刷参考图标定；这些结果不代表独立识别准确率、30–50cm实机追踪、微信兼容或最终动画审美验收。是否实际发布须以对应提交的 Pages 部署结果为准。
