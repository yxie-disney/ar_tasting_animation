# 当前：五帧硬切，不做新画、不做转场

用户最新决定取代此前植物建模和图生视频路线。

- 五张朏朏复用现有扫描衍生透明 PNG。五份 PSD 是整个人物层，不是可动部件稿。
- 从七张品鉴卡中任选五张，整图展示。所有十张 PNG 按字节原样复制；透明品鉴卡在运行时合成白纸底，不裁切、不改字、不调色。
- 配对和替换入口：`site/ar/assets/slides/manifest.json`。配对仅为此次 prototype，不是正式 SKU/角色关系。
- 实体试管 → 朏朏 → 背景虚拟卡，沿已确认观看方向从近到远。`stage.js` 保持不变。
- 朏朏高度为场景单位 80mm，保持原图比例；底边与卡片底边同高。深度放在管后与卡前的中间位置。
- 管长 215 个场景单位分五区，站位取区中心；从观看者左至右，每秒换角色并跳一格。背景卡仅换贴图，不动、不缩放。
- 同一时钟驱动全部切换：0–1s 第一帧，1–2s 第二帧，至4–5s第五帧，然后循环。沿用追踪状态：失去可信定位时暂停可见时间，恢复后续播；不假称无定位也能稳定显示。
- 没有插值、转场、角色表演、音乐、新物料或新平台订阅。

下一轮可以单独制作五帧新美术，再替换图；转场在之后单独实现，不牵动这轮的资产/播放分工。
